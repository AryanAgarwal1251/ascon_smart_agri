"""Vendored third-party code, kept byte-identical to upstream for auditable re-verification.

Nothing in this package is authored here. Each subpackage records its upstream source, pinned
commit, and license in its own ``__init__`` docstring and ``PROVENANCE.md``. These files are
excluded from the repo's lint/type/dead-code gates (see ``pyproject.toml``) precisely so they
stay verbatim: re-verification is ``diff``-against-upstream, not local editing.
"""
